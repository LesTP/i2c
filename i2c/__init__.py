"""i2c — Idea to Code.

A governance framework for AI-driven software development: structured on-disk
state and pre-assembled worker prompts. This package bundles the deterministic
tooling (state writer, schema validator, context assembler, state machine,
invariant checks, single-iteration runner), the in-process command API
(`control`), and the `i2c` console dispatcher (`cli`).
"""

from __future__ import annotations

__version__ = "0.1.0"
